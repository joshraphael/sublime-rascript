on:
  push:

name: Test Release Files
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup env
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: make release